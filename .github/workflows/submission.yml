name: Submission

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  submission:
    name: Automated submission checks
    uses: YOUR-ORG/web-programming-ci-utils/.github/workflows/submission.yml@0000000000000000000000000000000000000000
    with:
      utils_repository: YOUR-ORG/web-programming-ci-utils
      utils_ref: "0000000000000000000000000000000000000000"
